\section{Method}
\label{sec:method}

\subsection{System overview}
Four modules run concurrently: visual SLAM (front + back end),
1D-rangefinder filter, state estimator, and an MPC controller.
A fifth module -- the Gemini Nano reasoner -- runs at low rate and
publishes advisory mission updates.

\subsection{Metric scale from a 1D beam}
Let $r_k$ be the measured range and $\hat r_k$ the SLAM-predicted range
to the local ground plane (intersected with the beam). Over a sliding
window of $W$ samples we estimate the scale
\[
  s = \mathrm{mean}\bigl\{\, r_k / \hat r_k \mid k \in \mathcal{I} \,\bigr\},
\]
where $\mathcal{I}$ is the inlier set under a relative-tolerance test
against the median. The estimated $s$ is multiplied into the SLAM map
and pose stream once it stabilizes.

\subsection{Perception-aware MPC with acoustic clearance constraint}
\label{sec:mpc}
Let $x_k = [p_k^\top, v_k^\top, q_k^\top, \omega_k^\top]^\top$ be the
discrete-time state at horizon step $k \in \{0, \dots, N-1\}$ and
$u_k = [T_k, \omega_k^{\text{cmd}\,\top}]^\top$ the collective thrust
and body-rate setpoint. We solve, at each control tick,
\begin{align}
\min_{\substack{x_{0:N},\\u_{0:N-1}}} \;& \sum_{k=0}^{N-1}
    \| x_k - x_k^{\text{ref}} \|_{Q(t)}^2
  + \| u_k \|_{R}^2
  + w_p(t)\,\phi(x_k, \mathcal{M}, \mathbf{b}) \nonumber\\
\text{s.t.}\;& x_{k+1} = f(x_k, u_k), \quad x_0 = \hat x \label{eq:dyn}\\
            & u_k \in \mathcal{U} \label{eq:input}\\
            & \|v_k\| \leq v_{\max}(t)               \label{eq:vmax}\\
            & d_b(x_k) \geq c^{\min}_b - \epsilon_b
              \quad \forall b\!\in\!\mathcal{B}_{\text{active}}(t)
              \label{eq:acoustic}
\end{align}
where $\phi$ penalizes predicted feature loss (visibility of map
points $\mathcal{M}$) and beam-invalid geometry (the 1D beam
$\mathbf{b}$ leaving its planar surface), $d_b(\cdot)$ is the
predicted body-axis clearance toward direction $b$, and
$\mathcal{B}_{\text{active}}(t)$ is the set of directions for which
the acoustic backup's confidence currently exceeds the gate
$\eta$. The advisor (Sec.\,\ref{sec:reasoning}) modulates $Q(t)$,
$w_p(t)$, and $v_{\max}(t)$, and the acoustic node populates $c^{\min}_b$
and $\mathcal{B}_{\text{active}}(t)$. Slacks $\epsilon_b \geq 0$ are
penalized in the objective so the QP stays feasible at high
disturbance, but the supervisor refuses any solution with
$\epsilon_b > 0$ if a hard constraint is required by mission state.
Default weights are listed in Table~\ref{tab:mpc-params}.

\subsection{Acoustic backup proximity}
A passive microphone array on the airframe captures the drone's
ego-noise (propeller wash) reflected from nearby surfaces. After
band-pass filtering to the propeller harmonic band and ego-reference
whitening, cross-correlation across mic pairs yields per-direction
proximity scores $\rho_d \in [0,1]$ for $d \in \{\text{front, back,
left, right, up, down}\}$, and where consistent across pairs, a
time-of-flight clearance estimate $c_d$.

This signal is a *backup*: while SLAM tracking is healthy it is
ignored. When SLAM degrades or its estimated covariance grows beyond a
threshold, the MPC consumes $c_d$ as a hard lower bound on body-axis
clearance, and the reasoner sees $\rho_d$ in the state summary so it
can prefer retreat directions away from the most-occluded axis. This
keeps the drone safe through brief perception outages without
requiring any extra ranging emitter.

\subsection{On-device reasoning loop}
\label{sec:reasoning}
The reasoner replaces what would otherwise be a hand-written rule tree
over sensor states. We split it into three layers (Fig.\,\ref{fig:reasoning},
described in detail in \texttt{docs/llm\_decision\_design.md}):

\begin{enumerate}
\item A \textbf{Monitor} aggregates raw signals into a structured
  observation $\mathbf{o}_t$: pose, velocity, SLAM tracking flag and
  inlier \emph{trend}, range valid-ratio, per-direction acoustic
  proximity, battery, IMU vibration, motor saturation, and mission
  state. The Monitor performs no policy logic.
\item An \textbf{SLM advisor} (Gemini Nano on Pixel; a Gemini
  Nano-class SLM such as Gemma\,3\,1B via \texttt{llama.cpp} on
  Pi\,5+AI HAT) consumes $\mathbf{o}_t$ and emits a structured
  action $a_t = \{\text{mode}, \Delta\text{wp}, v_{\max},
  \text{yaw}, \text{mpc\_weights}, \text{sensor\_trust},
  \text{replan}, \text{rationale}, \text{confidence}\}$. The action
  schema lets the SLM retune MPC weights and per-modality fusion
  trusts, not just request a mode change.
\item A \textbf{SafetySupervisor} clamps every field of $a_t$ to a
  verified envelope (mode whitelist, speed cap, waypoint-delta norm,
  altitude floor, geofence, weight and trust ranges). Battery
  thresholds override the SLM (forced \emph{return} or \emph{land}).
  The supervisor emits a clamp report so we can audit overrides
  offline.
\end{enumerate}

The SLM is advisory and runs at $0.5$--$2$\,Hz; the SafetySupervisor
and the MPC's own constraints are the safety authority. This split
keeps the hard real-time path classical and verifiable while letting
the SLM collapse a high-dimensional state matrix into a contextual
decision without us writing the conjunction by hand.

% Wrapped in figure rather than algorithm so it compiles with only
% the algorithmic package (no algorithm float wrapper required).
% Add \usepackage{algorithm} and switch back to algorithm[t] when
% finalizing the camera-ready.
\begin{figure}[t]
\centering
\begin{minipage}{0.95\linewidth}
\begin{algorithmic}[1]
\REQUIRE Latest sensor messages, mission waypoint $w$, current
         estimate $\hat x$, battery $b$.
\STATE $\mathbf{o}_t \gets \textsc{Monitor.observe}(\hat x, \text{sensors}, w)$
       \hfill \COMMENT{trends, ratios, time-since}
\STATE $a_t \gets \textsc{Advisor.decide}(\mathbf{o}_t)$
       \hfill \COMMENT{SLM or rule-tree, same schema}
\STATE $a_t^\star, R \gets \textsc{Safety.apply}(a_t, \hat x, b)$
       \hfill \COMMENT{$R$: clamp report}
\IF{$R.\text{clamped}$}
  \STATE log $R.\text{reasons}$ for offline audit
\ENDIF
\STATE publish $w \gets w + a_t^\star.\Delta\text{wp}$;
       $v_{\max} \gets a_t^\star.v_{\max}$;
       $Q(t) \gets Q_0 \odot a_t^\star.\text{mpc\_weights}$;
       $T(t) \gets a_t^\star.\text{sensor\_trust}$;
       $\text{mode} \gets a_t^\star.\text{mode}$.
\STATE \textbf{guarantee} $\|v_k\| \leq v_{\max}^{\text{hard}}$,
       $z_k \geq z^{\min}$, geofence(\,$p_k$\,) regardless of $a_t$.
\end{algorithmic}
\end{minipage}
\caption{Per-tick advisory pipeline.}
\label{alg:advisor}
\end{figure}

\subsection{Implementations}
\label{sec:impl}
We provide two complementary implementations of the stack. The
\textbf{ROS\,2 + Gazebo Harmonic} build runs the full nonlinear MPC
in CasADi and bridges sensor topics through \texttt{ros\_gz\_bridge}
(\texttt{launch/sim.launch.py}). For paper-grade ablations we also
provide a \textbf{Python-only simulator} under
\texttt{experiments/sim/} that drives the same library code -- the
Monitor, both advisors, and the SafetySupervisor -- against
synthetic dynamics, sensors, and degradation schedules. The
Python sim closes the loop with an analytical LQR controller
(\texttt{gps\_denied\_drone/control/lqr.py}) that honors the
advisor's $v_{\max}$, weight, and mode outputs in the same way the
Gazebo MPC does. This makes paper experiments reproducible on a
laptop in seconds and removes Gazebo as a confounder when
isolating the advisor's contribution.
